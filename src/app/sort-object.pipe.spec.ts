import { SortObjectPipe } from './sort-object.pipe';

describe('SortObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new SortObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
